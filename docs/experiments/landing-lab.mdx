import ExperimentIsland from '../src/components/experiments/ExperimentIsland.tsx';

Landing is a controller problem with delayed thrusters, wind, and a finite fuel budget. The search selects a controller from a fixed, inspectable grammar.

Compare the baseline with the traces from the selected controller. Brighter paths are safe landings in the toy physics model.

<ExperimentIsland client:load demo="landing-lab" />
