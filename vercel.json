{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm --dir docs install --frozen-lockfile",
  "buildCommand": "pnpm --dir docs run build",
  "outputDirectory": "docs/dist"
}
